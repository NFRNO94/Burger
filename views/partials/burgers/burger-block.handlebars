<li>
	<button class="change-devoured btn-dark btn-lg" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}{{name}}{{else}}{{name}}{{/if}}
	</button>

	<button class="delete-burger btn-outline-dark btn-sm bg-warning" data-id="{{this.id}}">DELETE!</button>
	<br>
	<br>
</li>